# 本地开发配置示例
# 复制本文件为 application-local.yaml，填入真实值，该文件已被 .gitignore 忽略，不会提交到仓库
spring:
  datasource:
    username: root
    password: "your-db-password"

tourwise:
  oss:
    access-key-id: "your-aliyun-ak"
    access-key-secret: "your-aliyun-sk"
  ai:
    api-key: "your-deepseek-api-key"
